# Phase 0 dev env. Real secrets land in Doppler in Phase 1+.
# Copy to .env for local docker-compose dev.

DATABASE_URL=postgresql+asyncpg://saaspoly:saaspoly@localhost:5432/saaspoly
REDIS_URL=redis://localhost:6379/0
SMTP_HOST=localhost
SMTP_PORT=1025

# Set by docker-compose; override locally if needed.
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO
