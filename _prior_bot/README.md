# `_prior_bot/`

Reference implementations for the strategies marked `port` in §12.2 of the
build spec live here. Nicola will upload the prior bot source into this
directory.

Until that happens, **no Phase 1 strategy work can start** — Phase 1 step 9
(§15) lifts `ninety_cent` from this tree.

This directory is intentionally not a Python package and is excluded from
mypy / ruff / pytest discovery (see `pyproject.toml`).
